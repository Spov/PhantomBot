exports.run = (client, message, args) => {
    message.author.sendMessage("\nWe currently don't have a full list of requirements."
    	    + " since we haven't published our guild introduction yet. Message a team"
		    + "member to assist you further." );
};